# Bash completion script for logbuch tools

Place this file in /etc/bash_completion.d/logbuch and enjoy.

It completes these commands:
- `apt-install`
- `apt-remove`
- `apt-autoremove`
- `apt-build-dep`
- `apt-upgrade`
- `apt-dist-upgrade`

logbuch tools: https://hg.intevation.de/adminton/
